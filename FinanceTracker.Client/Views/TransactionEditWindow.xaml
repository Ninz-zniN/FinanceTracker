<Window x:Class="FinanceTracker.Client.Views.TransactionEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FinanceTracker.Client.Views"
        mc:Ignorable="d"
        Title="Транзакция" Height="450" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Сумма" Margin="0,0,0,5"/>
            <TextBox x:Name="AmountBox" Margin="0,0,0,15"/>

            <TextBlock Text="Дата" Margin="0,0,0,5"/>
            <DatePicker x:Name="DatePicker" Margin="0,0,0,15"
                        SelectedDate="{x:Static system:DateTime.Today}"
                        xmlns:system="clr-namespace:System;assembly=mscorlib"/>

            <TextBlock Text="Тип" Margin="0,0,0,5"/>
            <ComboBox x:Name="TypeBox" Margin="0,0,0,15" SelectedIndex="1">
                <ComboBoxItem Content="Доход" Tag="Income"/>
                <ComboBoxItem Content="Расход" Tag="Expense"/>
            </ComboBox>

            <TextBlock Text="Категория" Margin="0,0,0,5"/>
            <ComboBox x:Name="CategoryBox" Margin="0,0,0,15"
                      DisplayMemberPath="Name"/>

            <TextBlock Text="Заметка" Margin="0,0,0,5"/>
            <TextBox x:Name="NoteBox" Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Отмена" Style="{StaticResource MaterialDesignFlatButton}"
                        Margin="0,0,10,0" Click="Cancel_Click"/>
                <Button Content="Сохранить" Style="{StaticResource MaterialDesignRaisedButton}"
                        Click="Save_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
