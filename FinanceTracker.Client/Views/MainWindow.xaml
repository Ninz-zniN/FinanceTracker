<Window x:Class="FinanceTracker.Client.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FinanceTracker.Client.Views"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Finance Tracker"
        Style="{StaticResource MaterialDesignWindow}"
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Боковое меню -->
        <StackPanel Grid.Column="0" Background="#F5F5F5">
            <TextBlock Text="Меню" FontSize="18" FontWeight="Bold"
                       Margin="15,20,15,20"/>

            <Button Content="Транзакции" Margin="10,5"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Click="Transactions_Click"/>
            <Button Content="Долги" Margin="10,5"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Click="Debts_Click"/>
            <Button Content="Отчёты" Margin="10,5"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Click="Reports_Click"/>
        </StackPanel>

        <!-- Контент -->
        <ContentControl x:Name="ContentArea" Grid.Column="1" Margin="10"/>
    </Grid>
</Window>
